# Subsumio - Environment Template (DO NOT COMMIT SECRETS)
# Copy to your local environment manager (Vercel project settings / local .env.local)
# Note: Repo-wide .gitignore blocks .env* files. Use .env.local locally or Vercel env vars.

# ---------------------------------------------
# Marketing (Next.js marketing app)
# ---------------------------------------------
# Public origin used to build links to the app
NEXT_PUBLIC_APP_ORIGIN=https://app.subsum.io

# Store / distribution links (optional)
NEXT_PUBLIC_SUBSUMIO_IOS_STORE_URL=
NEXT_PUBLIC_SUBSUMIO_ANDROID_STORE_URL=

# Quick-Check (server-side route)
# If QUICK_CHECK_UPSTREAM_URL is empty, quick-check returns an explicit error.
QUICK_CHECK_UPSTREAM_URL=
QUICK_CHECK_UPSTREAM_API_KEY=
QUICK_CHECK_UPSTREAM_TIMEOUT_MS=12000
QUICK_CHECK_UPSTREAM_RETRIES=2
QUICK_CHECK_UPSTREAM_RETRY_DELAY_MS=350

# Optional: used to sign a handoff payload into app/portal flows
QUICK_CHECK_HANDOFF_SECRET=
QUICK_CHECK_APP_ORIGIN=https://app.subsum.io

# ---------------------------------------------
# Auth Email (API stub on api.subsum.io)
# ---------------------------------------------
# Required in production for email OTP/magic-link sign-in.
RESEND_API_KEY=
RESEND_FROM_EMAIL=

# Required in production to sign + verify the pending OTP cookie.
# Use a long random string.
AUTH_OTP_SECRET=

# ---------------------------------------------
# Electron (optional)
# ---------------------------------------------
# Sentry (optional)
SENTRY_DSN=
SENTRY_RELEASE=
SENTRY_ORG=
SENTRY_PROJECT=
SENTRY_AUTH_TOKEN=

# Dev convenience
DEV_SERVER_URL=

# ---------------------------------------------
# Backend (NestJS server - optional for Vercel stub, required for full server)
# ---------------------------------------------
# Payments (Stripe price IDs; only required if payments module enabled)
STRIPE_PRICE_EXTRA_PAGES_MONTHLY=
STRIPE_PRICE_EXTRA_USERS_MONTHLY=
STRIPE_PRICE_PREMIUM_SUPPORT_MONTHLY=
STRIPE_PRICE_CUSTOM_TEMPLATES_ONETIME=
STRIPE_PRICE_MIGRATION_ONETIME=
STRIPE_PRICE_DEDICATED_INFRA_MONTHLY=
STRIPE_PRICE_AI_CREDITS_5M_MONTHLY=
STRIPE_PRICE_AI_CREDITS_20M_MONTHLY=

# DocuSign (only required if legal PDF signing enabled)
DOCUSIGN_CLIENT_ID=
DOCUSIGN_USER_ID=
DOCUSIGN_ACCOUNT_ID=
DOCUSIGN_PRIVATE_KEY_PEM=
DOCUSIGN_OAUTH_BASE_URL=https://account-d.docusign.com
DOCUSIGN_REST_BASE_URL=https://demo.docusign.net/restapi

# Copilot providers (optional; set the one you use)
COPILOT_OPENAI_API_KEY=
COPILOT_ANTHROPIC_API_KEY=
COPILOT_PERPLEXITY_API_KEY=
COPILOT_EXA_API_KEY=
COPILOT_FAL_API_KEY=

# Media / assets (optional)
UNSPLASH_ACCESS_KEY=

# Server identity (optional)
AFFINE_ENV=production

# ---------------------------------------------
# Resend
# ---------------------------------------------
# No Resend integration found in the current codebase.
# If you want Resend for transactional emails, we can add:
# RESEND_API_KEY=
# RESEND_FROM_EMAIL=...
