# Case Assistant Module

## Local test command

Run only the module-local tests without the monorepo workspace runner:

```bash
yarn vitest run --config vitest.local.config.ts
```

Run this command from:

`packages/frontend/core/src/modules/case-assistant`
